# Normalise line endings so the repository always stores LF, regardless of
# the operating system the files were edited on.
* text=auto eol=lf

# Binary files must never be converted.
*.pdf binary
*.jpg binary
*.png binary
*.ico binary
